rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // แต่ละผู้ใช้อ่าน/เขียนข้อมูลของตัวเองเท่านั้น
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}